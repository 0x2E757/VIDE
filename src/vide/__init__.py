"""VIDE — per-user code-server instances under systemd.

`__version__` is the project's ONLY revision identifier. The published history
is a single root commit, so `git describe` names nothing and a clone's SHA is
an artifact of publication rather than of the code — without this string a bug
report cannot say which VIDE it is about. Bump it in the same commit as the
change it describes.
"""

__version__ = "0.1.0"
